class BacklogMembersController < ApplicationController
	before_filter :authenticate, :only => [:show]

	def show	
		@backlog = Backlog.find(params[:id])		
		@members = @backlog.collaborators		
	end
end
