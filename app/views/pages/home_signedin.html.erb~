<section class="column">
	<h2 class="left-aligned-header">Backlogs</h2>
	<div class="box">		
		<div class="box-content">
			<% unless @user.backlogs.empty? %>
				<ul class="backlogs fc">
					<%= render @backlogs %>
				</ul>
				<%= will_paginate @backlogs %>
			<% end %>
		</div>
	</div>
</section>


<section class="column" id="new_backlog_form">
	<% unless @backlog.nil? %>
		<h2 class="box-heading">Create new backlog</h2>
		<div class="box">
				<div class="box-content">
					<%= render "backlogs/new_backlog_form" %>
				</div>
		</div>
	<% end %>
</section>
